import { registerPlugin } from "@yank-note/runtime-api"

const name = 'extension-math.editor.hotkey'

export default () => {
    registerPlugin({
        name,
        register: ctx => {

        }
    })
}