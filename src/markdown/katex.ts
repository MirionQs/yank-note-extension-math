import { registerPlugin } from "@yank-note/runtime-api"

const pluginName = 'extension-math-editor-markdown-katex'

export default () => {
	registerPlugin({
		name: pluginName,
		register: ctx => {

		}
	})
}