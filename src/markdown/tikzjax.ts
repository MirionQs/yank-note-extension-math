import { registerPlugin } from "@yank-note/runtime-api"

const name = 'extension-math.markdown.tikzjax'

export default () => {
    registerPlugin({
        name,
        register: ctx => {

        }
    })
}